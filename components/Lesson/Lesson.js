// AboutScreen.js
import React from "react";
import { View, Text, Button } from "react-native";

const LessonScreen = ({ courseData }) => {
  return (
    <View style={{ padding: 20 }}>
      <Text style={{ fontSize: 24, fontWeight: "bold" }}>レッスン</Text>
    </View>
  );
};

export default LessonScreen;
